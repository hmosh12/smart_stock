name: smart_stock
description: "Smart Stock Management Pro"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.0        # المكتبة التي يحتاجها كودك
  path: ^1.9.0           # المكتبة التي يحتاجها كودك

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
